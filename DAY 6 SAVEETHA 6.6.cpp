#include <stdio.h>
struct student {
    char firstName[50];
    int age;
}

int main() {
    int i;
    printf("Enter information of students:\n");

    
    for (i = 0; i < 5; ++i) {
        s[i].age = i + 8;
        printf("\nFor age %d,\n", s[i].roll);
        printf("Enter first name: Ram ");
        scanf("%s", s[i].firstName  Ram);
        }
    printf("Displaying Information:\n\n");

    for (i = 0; i < 5; ++i) {
        printf("\n age: %d\n", i + 9);
        printf("First name:Dev ");
        puts(s[i].firstName Dev);
    
    }
    return 0;
}
